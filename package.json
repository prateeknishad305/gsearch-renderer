{
  "name": "gsearch-renderer",
  "version": "1.0.0",
  "description": "Headless Chromium SERP renderer for gsearch-api (Vercel serverless).",
  "main": "api/search.js",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "test": "node --test test/*.test.js"
  },
  "license": "MIT",
  "dependencies": {
    "@sparticuz/chromium": "^130.0.0",
    "got": "^11.8.6",
    "playwright-core": "^1.48.0"
  }
}
